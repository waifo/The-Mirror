import {
  allStories,
  storyById,
  primaryStories,
  editorStories,
  recentStories,
  stories,
} from "./stories";

const StoryQueries = {
  Query: {
    allStories,
    storyById,
    primaryStories,
    editorStories,
    recentStories,

    stories,
  },
};

export default StoryQueries;
