import {
  allStories,
  storyById,
  primaryStories,
  recentStories,
  stories,
} from "./stories";

const StoryQueries = {
  Query: {
    allStories,
    storyById,
    primaryStories,
    recentStories,
    stories,
  },
};

export default StoryQueries;
