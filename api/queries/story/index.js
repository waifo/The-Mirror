import { allStories, primaryStories, recentStories, stories } from "./stories";

const StoryQueries = {
  Query: {
    allStories,
    primaryStories,
    recentStories,
    stories,
  },
};

export default StoryQueries;
