{
  "name": "the-mirror",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "yarn workspace frontend start",
    "build": "yarn workspace frontend build",
    "test": "yarn workspace frontend test",
    "eject": "yarn workspace frontend eject"
  }
}
